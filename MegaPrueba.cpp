#include <iostream>
#include <string>
using namespace std;

int cuadrado(int n) {
    if (contador < 5) {
    auto valor = cuadrado(contador);
    cout << valor << endl;
    cout << valor << endl;
    cout << valor << endl;
    contador = contador + 1;

int main() {
    int contador;
    int valor;

        contador = 0;
    return 0;
}